{
  "name": "test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "publish": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.5.0"
  }
}
